$LOAD_PATH << 'lib'
$LOAD_PATH << 'spec'
require 'skype'

describe Skype::Group do
  before(:each) do
    pending 'constructor is broken'
    @group = Group.new
  end

  it "should desc" do
    # TODO
  end
end

